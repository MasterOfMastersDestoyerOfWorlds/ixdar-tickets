{
    "id": "MESH-1",
    "epic": "MESH",
    "repo": "Ixdar",
    "subsystem": [
        "geometry"
    ],
    "title": "Half-Edge Mesh Data Structure",
    "description": "Implement a half-edge mesh data structure as the core geometry representation for the mesh nodes system. This replaces the existing Face/FaceState model with a proper topology-aware structure that supports efficient adjacency queries (vertex neighbors, edge loops, face corners). The half-edge mesh is foundational -- every mesh node will produce or consume this type. Must support: vertex/edge/face creation and deletion, splitting, merging, normal computation, boundary detection, and iteration over all adjacency relationships. Should be designed for large meshes (100k+ faces) with O(1) adjacency lookups. NOTE: This ticket can be developed in parallel with MESH-5 (the viewer scene). MESH-5 uses a hardcoded cube for initial rendering; this ticket provides the real data structure that MESH-4 will render.",
    "blocked-by": [],
    "blocks": [
        "MESH-2",
        "MESH-4",
        "MESH-6",
        "MESH-9",
        "MESH-10",
        "MESH-11",
        "MESH-12",
        "MESH-22",
        "MESH-23",
        "MESH-24",
        "MESH-25",
        "MESH-26",
        "MESH-27"
    ],
    "status": "DONE",
    "definition-of-done": "1. HalfEdgeMesh, HalfEdge, Vertex, Edge, MeshFace classes exist in ixdar-app/src/main/java/ixdar/geometry/mesh/. 2. Adjacency queries work in O(1): vertex->outgoing half-edges, half-edge->twin/next/prev/face/vertex, face->half-edges, edge->half-edges. 3. Can build a cube and icosphere from raw vertex/face data and traverse all adjacency. 4. Supports add/remove vertex, edge, face operations. 5. Computes per-face and per-vertex normals. 6. Boundary edges are detectable. 7. No per-frame allocations in traversal methods.",
    "testing-plan": "1. Unit test: build a cube (8 verts, 12 tris), verify adjacency counts. 2. Unit test: build an icosphere, verify Euler characteristic V-E+F=2. 3. Unit test: delete a face, verify adjacency updates. 4. Unit test: boundary detection on a plane (open mesh). 5. Unit test: normal computation matches expected directions. 6. Stress test: build a 100k-face subdivided sphere, verify no OOM and traversal completes in <100ms.",
    "todos": [
        "DONE : Replace the object-reference prototype with a packed, index-based HalfEdgeMesh plus storage-neutral MeshTopology API",
        "DONE : Implement addVertex, addFace, addEdge operations that maintain invariants",
        "DONE : Implement adjacency traversal APIs for vertices, faces, edges, and half-edges",
        "DONE : Implement removeFace with adjacency cleanup",
        "DONE : Implement computeNormals (per-face and per-vertex via area-weighted average)",
        "DONE : Implement boundary edge detection",
        "DONE : Implement buildFromIndexedMesh(float[] positions, int[] faceIndices) factory",
        "DONE : Add unit tests for topology, normals, boundary handling, compiled surface export, and large procedural grid builds",
        "DONE : Defer explicit split/merge editing primitives to later mesh operation/node tickets rather than blocking the packed mesh foundation"
    ],
    "unknowns": [
        "Whether explicit split/merge topology editing primitives should be implemented in MESH-1 or deferred to later mesh operation tickets",
        "How much of the compiled surface export should move into MESH-4 versus stay as a reusable CPU-side DTO boundary in geometry/render"
    ],
    "changes-made": [
        "Added packed, index-based HalfEdgeMesh storage with paired boundary half-edge invariants and stable IDs.",
        "Added MeshTopology as the storage-neutral public traversal API and moved mutation/validation work into HalfEdgeMeshEngine.",
        "Added HalfEdgeCompiledMeshData as a compiled surface boundary for later runtime integration.",
        "Replaced the old object-reference mesh tests with ID-based coverage for cube adjacency, Euler characteristic, boundary detection, face deletion, non-manifold rejection, compiled export, and a 100k-face-class procedural grid."
    ],
    "related-files": [
        "ixdar-app/src/main/java/ixdar/geometry/mesh/HalfEdgeMesh.java",
        "ixdar-app/src/main/java/ixdar/geometry/mesh/HalfEdgeMeshEngine.java",
        "ixdar-app/src/main/java/ixdar/geometry/mesh/MeshTopology.java",
        "ixdar-app/src/main/java/ixdar/geometry/mesh/IntIdList.java",
        "ixdar-app/src/main/java/ixdar/graphics/render/model/HalfEdgeCompiledMeshData.java",
        "ixdar-app/test/unit/mesh/HalfEdgeMeshTest.java"
    ],
    "priority": 1
}